/// <summary>
/// Small class container for StatTypes
/// </summary>

public enum StatType    // Collection of Stattypes to act as choices for selections
{
    hitPoints, damage, moveSpeed, maxSpeed, friction, fireRate, bulletSpeed
}